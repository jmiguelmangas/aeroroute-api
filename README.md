# aeroroute-api

FastAPI application for public API orchestration, persistence, weather adapters,
and result history. It consumes `aeroroute-optimizer`; it never duplicates its
route physics.

## Airport data import

Prefer an immutable bundle generated by `aeroroute-data`:

```bash
uv run aeroroute import-airports --bundle ../airport-bundle
```

The legacy `--file airports.csv` command remains available for local migration.
